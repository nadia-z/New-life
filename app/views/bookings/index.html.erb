<h1 class="m-5">Looking forward to your next experience?</h1>
<h5 class="m-5">Your bookings</h5>
<div class="m-5">
<div class="six-columns">
  <!-- Header row -->
  <p></p> <!-- for image column -->
  <h5>Title</h5>
  <h5>Start date</h5>
  <h5>End date</h5>
  <h5>Total price</h5>
  <p></p> <!-- for edit button -->

  <% @bookings.each do |booking| %>
    <%= image_tag 'Moritz_von_Nassau.png', alt: 'image', width: '100' %>
    <h5><%= booking.life.title %></h5>
    <p><%= booking.start_date %></p>
    <p><%= booking.end_date %></p>
    <p><%= "€ #{booking.total_price.to_i}" %></p>
    <%= link_to "Show Booking", life_booking_path(booking.life, booking), class: "btn btn-primary mt-5" %>
  <% end %>
</div>
</div>
